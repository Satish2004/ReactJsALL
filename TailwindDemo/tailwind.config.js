/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["*"],
  // if i consider all HTML file then simply write "*" rather then specific name
  theme: {
    extend: {},
  }, 
  plugins: [],
}

